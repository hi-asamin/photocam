import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

// 福井作成中
export const CampingFoodScreen = () => (
  <View style={styles.container}>
    <Text>キャンプ飯</Text>
  </View>
);

const styles = StyleSheet.create({
  container: {
    // flex: 1,
    backgroundColor: '#FFFFFF',
  },
});
