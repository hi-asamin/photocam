import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

// 福井作成中
export const CampingFoodDetailScreen = () => (
  <View style={styles.container}>
    <Text>キャンプ飯詳細</Text>
  </View>
);

const styles = StyleSheet.create({
  container: {
    // flex: 1,
    backgroundColor: '#FFFFFF',
  },
});
